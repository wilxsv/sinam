 <section id="services" class="services">
 <div id="comments" class="comments">
  <div class="comments-area">
   {% set max = 5 %}
   {% set id = 0 %}
   {% if rest == true %}
    {% set med = 0 %}
    {% for item in rest %}
    {% set id = id + 1 %}
    {% if med == 0 %}
      <h3 class="comment-title">{{ item.nombre }}</h3>
      <p>Descripción: {{ item.formafarmaceutica}}<br/>
      Presentación: {{ item.presentacion}}</p>
      {% for m in siap %}
            <p><b>{{ m.establecimiento }} / Farmacia: {{ m.farmacia }} (SIAP)</b><br/>
             {{ m.existencia|number_format(0, '.', ',') }} {{ m.unidad }} (SIAP)
            </p>
      {% endfor %}
      {% set med = 1 %}
    {% endif %}
    {% if id < max %}
      <div class="comments-details">
        <ul class="comment-list">
         <li class="parent">
          <article class="comment">
           <div class="comment-author"></div>
            <div class="comment-content">
            <h4 class="author-name">Establecimiento: {{ item.establecimiento }} (SINAB)</h4>
            <p>Municipio: {{ item.municipio }}, Departamento: {{ item.depto }}<br/>
            <b>Existencias: {{ item.cantidaddisponible|number_format(0, '.', ',') }} {{ item.unidad }} [Almacén: {{ item.almacen }}]</b>
            </p>
          </div>
          </article>
         </li>
        </ul>
      </div>
    {% endif %}
    {% endfor %}
   {% else %}<div class="item">
      <div class="row">
      <p align="center">
       <h4 class="author-name">No se encontraron existencias</h4>
       <p>No se pudo encontrar el medicamento búscado, Intente repetir la búsqueda utilizando los filtros o consulte los medicamentos de la tabla de medicamentos alternativos para hacer una nueva búsqueda.</p>
       <table style="width:100%">
         <tr><th><b>Medicamento</b></th><th><b>Medicamento Alternativo</b></th></tr>
         {% for item in alt %}
          <tr><td>{{ item.nombre }}</td><td>{{ item.alternativa }}</td></tr>
         {% endfor %}
       </table> 
      </p>
      </div>
     </div>
   {% endif %}
   </div>
  </div>
 </section>
